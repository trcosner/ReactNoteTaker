import React from 'react';

//if component doesnt have any state and only has render method
//we can use statless function components

const Home = () => {
  return(
    <h2 className="text-center">
      Search By Github Username Above
    </h2>
  )
}

export default Home
